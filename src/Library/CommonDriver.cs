namespace Library
{
    public class CommonDriver : Driver
    {
        public CommonDriver(string calification, string vehicle, string biografy, string name, string lastname,
            string ID) :
            base(calification, vehicle, biografy, name, lastname, ID)
        {

        }
    }
}
